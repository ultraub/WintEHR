# Core dependencies
fastapi>=0.140.0
uvicorn[standard]>=0.51.0
pydantic>=2.5.0
# EmailStr in api/auth/models.py needs this. It used to arrive transitively via
# fhir.resources, which was an unused shim and has been removed — so the
# dependency is now declared where it is actually used.
email-validator>=2.3.0
sqlalchemy[asyncio]>=2.0.23

# WebSocket support
websockets==16.1.1

# Database
alembic>=1.12.1
asyncpg>=0.30.0
psycopg2-binary>=2.9.12

# Cache
redis[hiredis]>=5.0.0

# HTTP/API
httpx==0.28.1
aiofiles==23.2.1
aiohttp==3.14.3

# Data processing
pandas>=2.2.0
ndjson==0.3.1

# System monitoring
psutil==5.9.6

# Security
python-jose[cryptography]==3.5.0
python-multipart==0.0.32
# Used directly (api/auth/jwt_handler.py) — passlib was dropped: unmaintained
# since 2020 and permanently incompatible with bcrypt >= 5.
bcrypt==5.0.0
PyJWT==2.13.0

# Development tools
# pytest 9 + pytest-asyncio 1.x move TOGETHER: asyncio 0.21 pins pytest<8 and
# under pytest 9 it silently no-op'd every async test (the incident that gave
# CI its outcome-count gate — see .github/workflows/ci.yml).
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
aiosqlite>=0.19.0  # Async SQLite for test fixtures

# FHIR specific
fhirclient==4.4.0  # SMART on FHIR Python client for HAPI FHIR integration

# Synthea integration
GitPython==3.1.57

# Medical imaging
pydicom==3.0.2
pillow>=12.3.0
numpy>=1.26.0
scipy>=1.18.0  # For realistic DICOM generation
# For DICOM image processing (basic support)
pylibjpeg==2.1.0
pylibjpeg-libjpeg==2.4.0
# pylibjpeg-openjpeg==2.0.0  # Has CMake compatibility issues with newer versions

# AI and utilities
anthropic==0.120.0
python-dotenv==1.2.2
# LLM Providers
openai>=1.0.0
google-generativeai>=0.3.0
